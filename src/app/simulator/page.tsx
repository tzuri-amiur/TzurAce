import Simulator from './Simulator';

export const metadata = {
    title: 'TzurAce | Simulator',
    description: 'Full poker table simulator with dynamic seats and live controls.',
};

export default function SimulatorPage() {
    return <Simulator />;
}
